{
  "name": "smart-code-reviewer",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run dev:api & bun run dev:web & wait",
    "dev:api": "bun run --cwd apps/api dev",
    "dev:web": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build && rm -rf .svelte-kit/cloudflare && mkdir -p .svelte-kit && cp -R apps/web/.svelte-kit/cloudflare .svelte-kit/cloudflare",
    "test": "bun test --cwd apps/api",
    "check": "bun run --cwd apps/api typecheck && bun run --cwd apps/web check"
  }
}
